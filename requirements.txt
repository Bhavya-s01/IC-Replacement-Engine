# ═══════════════════════════════════════════
# IC Alternative Finder — Python Dependencies
# Install: python -m pip install -r requirements.txt
# ═══════════════════════════════════════════

# ── Scraper (DigiKey) ──
playwright>=1.40.0

# ── API Backend ──
fastapi>=0.100.0
uvicorn>=0.20.0
pydantic>=2.0.0

# ── Datasheet Parser ──
pymupdf>=1.23.0
requests>=2.28.0

# ── Data Export ──
pandas>=2.0.0

# ── Scraper Fallback (optional) ──
curl-cffi>=0.6.0
beautifulsoup4>=4.12.0
lxml>=5.0.0

# ── CLI Formatting (optional) ──
rich>=13.0.0
click>=8.1.0